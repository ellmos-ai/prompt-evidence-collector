{
  "schema": "ellmos.module.v2",
  "id": "prompt-evidence-collector",
  "display_name": "prompt-evidence-collector",
  "category": "memory",
  "kind": "service",
  "status": "active",
  "visibility": "private",
  "provides": [
    "evidence.prompt.local-store",
    "evidence.receipt.hashed",
    "evidence.consumer.locator",
    "evidence.capture.authorized.one-shot"
  ],
  "requires": [],
  "optional": [
    "policy-registry"
  ],
  "conflicts": [],
  "surfaces": [
    "library",
    "cli"
  ],
  "state": {
    "ownership": "external",
    "location": "project"
  },
  "boundaries": {
    "network": "none",
    "data": "sensitive",
    "platforms": [
      "windows",
      "macos",
      "linux"
    ]
  },
  "entrypoints": {
    "cli": "prompt-evidence-collector doctor",
    "library": "prompt_evidence_collector"
  },
  "source_of_truth": {
    "type": "git-repository",
    "repository": "https://github.com/ellmos-ai/prompt-evidence-collector",
    "path": "."
  },
  "notes": "Standalone extraction from ellmos-core@03f6f58. Raw prompt/session text never leaves the local store; outbound artifacts are hashed receipts only. Default-passive: no scheduler, no network and no automatic promotion. Live locator capture requires a signed one-shot CaptureGrant, a trusted authority key, an immutable signed resolver-runtime receipt and an atomic replay ledger. Explicit user decisions, capture policies and delegated decision-avatar grants are supported as typed authority sources, but the latter authorizes only after Policy-Registry trust provisioning; TOM_lm prediction alone never suffices (V4-08 contract)."
}
